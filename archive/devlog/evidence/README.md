# Evidence files

Raw before/after logs backing quantitative claims in `../../DEVLOG.md`.

Naming: `YYYY-MM-DD_short-description_before.log` / `_after.log`

Every number in DEVLOG.md must trace to a file here. No file, no number.
