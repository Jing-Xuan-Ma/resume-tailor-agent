# UI Polish Plan — JobRight main path

Goal: ranked jobs → detail → tailor → confirm → apply, easy to operate without a manual.

## Batches
- UI-1 Jobs list clarity
- UI-2 Job detail + primary CTA
- UI-3 Resume workspace step rail
- UI-4 Shell nav / home entry
- Screenshot + self-critique until pass
