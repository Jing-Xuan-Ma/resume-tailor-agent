/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Intern-list viewer is proxied by app/intern-list-viewer/[[...path]]/route.ts
};

export default nextConfig;
